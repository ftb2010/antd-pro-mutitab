import { PageContainer } from "@ant-design/pro-components";
import React, { FC } from "react";

const Setting: FC = function () {
  return <PageContainer pageHeaderRender={false}>设置页</PageContainer>;
};

export default Setting;